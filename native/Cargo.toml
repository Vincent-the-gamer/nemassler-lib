[package]
name = "nemassler-lib"
description = "A Node.js library of nemassler, built in Rust."
version = "0.1.1"
license = "MIT"
edition = "2021"
exclude = ["index.node"]

[lib]
crate-type = ["cdylib"]

[dependencies]
ncmdump = "0.8.0"
neon = "1"
